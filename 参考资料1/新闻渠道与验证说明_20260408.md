# 新闻渠道与验证说明（2026-04-08）

## 1) 当前站点爬虫（6 个）

来自 `project/ai_news_skill/crawlers/site/registry.py`：

1. OpenAI Blog
2. Anthropic Blog(OpenRSS)
3. Hugging Face Blog
4. TechCrunch AI
5. Techmeme
6. VentureBeat AI

> 这些属于“站点爬虫通道”；是否执行还受 `SITE_CRAWLERS_ENABLED`、`DIGEST_FAST_MODE` 等开关影响。

---

## 2) 渠道口径（按你最新要求）

- Public API 仅保留：`nyt`
- GNews 归入 Public API 口径统计（虽然技术实现上是单独模块）
- 已将 `.env` 中 `PUBLIC_API_FEEDS` 改为：
  - `PUBLIC_API_FEEDS=nyt`

---

## 3) 本次实测（关键词 / 泛化）

测试环境：
- 使用 `.env` 现有配置（含 NYT key 与 GNews key）
- `public_api_feeds='nyt'`
- `gnews_enabled=true`
- `items_per_source_max=1`

测试意图与结果：

1. 关键词意图：`查 OpenAI Safety Fellowship`
   - 返回条数：1
   - 命中来源示例：OpenAI Blog
   - 说明：该关键词更偏向 OpenAI 官方单一事件，NYT/GNews 在该词下返回较少。

2. 泛化意图：`随便查查`
   - 返回条数：10
   - 命中来源示例（前几条）：OpenAI Blog、Anthropic Blog(OpenRSS)、The New York Times (API)、多个 GNews 来源
   - 说明：泛化输入可拉到 NYT 与 GNews 结果，渠道链路可用。

补充：
- 本次测试出现过 LLM 富化 404（模型端点配置问题）告警，但不影响抓取链路与报告生成。

---

## 4) 当前“新闻获取渠道”汇总（按业务口径）

### A. 主信源（sources.json）
- 共 9 个条目（RSS/站点入口）
- 其中站点爬虫实现 6 个（见第 1 节）

### B. Public API（按本次口径）
- NYT（启用）
- GNews（按你的要求并入 Public API 统计）

即：当前实际维护口径可视为 **主信源 + 2 个 Public API 渠道（NYT + GNews）**。
