# AI 资讯周报 Agent — 项目架构说明

> **文档定位**：本文件在**仓库根目录**，用中文 + 树状图概括**本仓库**结构；风格上对齐 `external/deer-flow/DeerFlow-architecture-ZH.md`（树状目录 + 模块说明）。  
> **更细的英文说明**（分层图、阶段细节、Tool 路径）：见 `project/docs/agent-architecture.md`。  
> **外部借鉴**：`external/` 下为第三方克隆，**不参与**本项目的运行时依赖。  
> **维护说明**：**本文件为中文架构总览的唯一正文**；同目录 `PROJECT-ARCHITECTURE-ZH.md` 仅为 **ASCII 文件名入口**（无重复正文），**请勿**在后者粘贴完整架构。若编辑器打不开中文路径，用资源管理器打开本文件或见该入口文件中的说明。

---

## 一、整体形态：是不是「多 Agent」？

**生产默认不是 DeerFlow 那种「主 Agent + task 子 Agent」树，而是「LangGraph 状态机 + 确定性流水线」。**

| 维度 | 说明 |
|------|------|
| **主路径** | `run_with_graph`：`StateGraph(DigestState)`，节点为 **bootstrap → intent → collect → enrich → write → persist**；失败时在 `collect` 前重试（带退避）。 |
| **LLM 角色** | 出现在 **意图拆词**（`intent_plan_stage`）、**条目解读与润色**（`enrich_stage`）等子步骤，而非全程由模型逐步选工具驱动。 |
| **可选路径** | LangChain **Tool-Calling / Agent**（见 `project/docs/agent-architecture.md` 中 Optional 章节）；失败时可回退到 LangGraph。 |

因此更准确的说法是：**单图编排 + 多阶段流水线**；站点爬虫、RSS、GNews、OpenClaw 等以**代码与配置**为主，而不是多个对话 Agent 并行协作。

---

## 二、仓库树状结构（核心与数据）

以下为**本仓库**逻辑树；省略 `.git`、`__pycache__` 等；`external/` 仅标一层。

```
ai-news-weekly-agent-main/
├── README.md                    # 产品说明、环境变量、快速命令
├── SKILL.md                     # OpenClaw Skill 说明（触发词、命令）
├── sources.json                 # RSS / 信源配置
├── requirements.txt
├── .env.example
├── daily_docs/                  # ★ 周报 Markdown 输出目录
├── runs/                        # ★ 单次运行 trace、run_id 子目录等
│
├── project/                     # ★ 主代码（运行时常以本目录为工作目录）
│   ├── run_daily_digest.py      # ★ 采集、GNews、去重、平衡、渲染 Markdown 核心脚本
│   ├── langgraph_agent.py       # 薄封装：从 skill 包重导出图与状态（便于根级 import）
│   ├── app.py                   # Streamlit UI 入口（exec 加载 ai_news_skill/ui/app.py）
│   ├── agent_runtime.py         # 重导出 ai_news_skill.runtime.agent_runtime
│   ├── digest_tools.py          # 与编排相关的工具注册 / 顶层 shim
│   ├── agent_tool_runner.py     # Tool Agent 路径（可选）
│   ├── mcp_bridge.py            # MCP 相关桥接（若启用）
│   │
│   ├── ai_news_skill/           # ★ 技能包形态的核心包
│   │   ├── runtime/
│   │   │   └── agent_runtime.py # ★ 流水线阶段：intent / collect / openclaw / enrich / write
│   │   ├── orchestration/
│   │   │   ├── langgraph_agent.py   # ★ StateGraph 定义与 run_with_graph
│   │   │   ├── digest_tools.py
│   │   │   └── agent_tool_runner.py
│   │   ├── pipeline/
│   │   │   └── run_daily_digest.py  # 对顶层 run_daily_digest 的再导出
│   │   ├── crawlers/
│   │   │   └── site/            # ★ 站点定制爬虫（OpenAI / Anthropic / TechCrunch 等）
│   │   ├── ui/
│   │   │   └── app.py           # Streamlit 表单、进度、调用图或脚本
│   │   └── integrations/
│   │       └── mcp_bridge.py
│   │
│   ├── middleware/
│   │   └── pipeline_middleware.py   # 流水线日志 / 事件
│   ├── model/
│   │   └── llm_factory.py           # LLM 构造
│   ├── prompts/
│   │   └── digest_llm_prompts.py    # 与 digest 相关的提示片段
│   ├── chains/
│   │   └── news_enrich_chain.py     # 富化链（若使用）
│   ├── site_crawlers/               # 站点爬虫的另一份实现/遗留（与 skill 内 crawlers 并存时注意维护策略）
│   ├── scripts/                     # 一次性脚本、样例
│   ├── test_harness_crawl/          # 爬取试验与样例输出
│   └── docs/
│       ├── agent-architecture.md    # ★ 英文：架构详解、数据流、阶段说明
│       └── restructure-root-clean.md
│
└── external/                    # 外部参考（如 deer-flow、FinnewsHunter）；非本产物依赖
```

**运行提示**：`SKILL.md` / `README` 中的 `python3 run_daily_digest.py ...` 一般指在 **`project/`** 下执行（或已将 `project` 加入 `PYTHONPATH`），以实际脚本路径为准。

---

## 三、编排与数据流（简图）

```
用户 / OpenClaw / Streamlit
        │
        ▼  组装 config（intent_text、窗口、OpenClaw 开关、LLM 等）
        │
┌───────┴───────────────────────────────────────────────────────┐
│  LangGraph：run_with_graph                                     │
│  bootstrap → intent → collect → enrich → write → persist → END   │
│       │        │        │         │        │                   │
│       │        │        │         │        └─→ daily_docs/*.md  │
│       │        │        │         └─→ LLM 解读 / 要点 / 标题等   │
│       │        │        └─→ collect_news + 站点爬虫 + GNews     │
│       │        │            + OpenClaw 榜单在成稿中位于正文条目之后（辅助参考）   │
│       │        └─→ intent_plan（可选 LLM 拆检索短语）            │
│       └─→ runs/<run_id>/、trace.json                           │
└───────────────────────────────────────────────────────────────┘
```

**与 `run_daily_digest.py` 的关系**：图中 **collect / enrich / write** 所依赖的去重、配比、渲染等大量逻辑在 **`project/run_daily_digest.py`**；**阶段顺序与 trace** 由 **`ai_news_skill/runtime/agent_runtime.py`** 与 **`orchestration/langgraph_agent.py`** 组织。

---

## 四、核心模块说明（按职责）

| 模块路径（相对 `project/`） | 中文职责 |
|----------------------------|----------|
| `run_daily_digest.py` | 信源加载、HTTP/RSS、GNews、条目规范化、去重与论文比例、Markdown 渲染、写文件。 |
| `ai_news_skill/runtime/agent_runtime.py` | 单次运行的 **阶段函数**：意图计划、采集阶段封装、OpenClaw、富化、写稿、finalize、可选 `run_digest_pipeline`。 |
| `ai_news_skill/orchestration/langgraph_agent.py` | **DigestState**、各 node、条件边（重试回 `collect`）、`build_workflow` / `run_with_graph`。 |
| `ai_news_skill/crawlers/site/*.py` | **站点定制**列表页/详情解析，优先于纯 RSS。 |
| `ai_news_skill/ui/app.py` | 本地 Web UI：配置运行参数、展示进度与结果路径。 |
| `middleware/pipeline_middleware.py` | 流水线步骤对外可观测性（与 `emit_step` 配合）。 |
| `model/llm_factory.py` | 统一创建聊天模型实例。 |
| `prompts/digest_llm_prompts.py` | Digest 相关 LLM 提示维护。 |

---

## 五、配置与产物

| 类型 | 路径 | 说明 |
|------|------|------|
| 信源列表 | 根目录 `sources.json` | RSS URL、分类等（运行时常与 `project` 工作目录一致，注意 cwd）。 |
| 环境 | `.env` / `.env.example` | Ark、OpenAI 兼容、可选 Webhook 等。 |
| 输出报告 | `daily_docs/` | 如 `ai_weekly_YYYYMMDD.md`。 |
| 运行痕迹 | `runs/<run_id>/` | `trace.json` 等，便于排查采集与 LLM 步骤。 |

---

## 六、维护建议

1. **改采集逻辑**：优先看 `run_daily_digest.py` 与 `crawlers/site/`。  
2. **改流程顺序或重试策略**：改 `ai_news_skill/orchestration/langgraph_agent.py`。  
3. **改阶段内行为（OpenClaw、富化）**：改 `ai_news_skill/runtime/agent_runtime.py`。  
4. **英文深度架构、Tool 回退路径**：保持与 `project/docs/agent-architecture.md` 同步更新。

---

*文档随当前仓库结构整理；若目录调整，请同步更新本节树状图。*


【记忆】
实现了什么
1. 检索模式 classify_query_mode(intent_text)（在 user_news_memory.py）
generic（泛化浏览）：整句像「看看最近新闻 / 最近有什么资讯 / 随便看看 / 来份周报」等，不带强主题时命中。
explicit（显式主题）：含 查 / 搜 / 检索 / find / search；或含 harness、engineering、arxiv、论文、多模态、openai 等主题线索；或 默认 当作「当前句自带主题」（例如「视频模型」会走 explicit，避免被当成泛化）。
2. attach_memory_to_config 按模式分支
generic	explicit
记忆里的 extra_keywords / prefer_categories / profile 拼进意图
✅
❌（清空，不合并进关键词与板块）
排除信源 excluded_source_substrings
✅
✅
工具 Agent 的 system 块
完整偏好块
仅「曾排除的信源关键词」
_user_memory_boost_*
从文件拷贝，用于重排
仍从文件读入，但 重排逻辑不生效（见下）
3. 泛化浏览下的 重排加权 rerank_items_by_user_memory_boost
仅在 _query_mode == "generic" 且记忆里有 boost 词/板块时执行。
对每条：标题+摘要+来源+板块 命中记忆关键词 +2 / 命中偏好板块 +1，再 稳定排序（同分保持原顺序）。
在 intent_stage 里、balance_items 之后调用，并打 intent_rank 步骤（带 mode: user_memory_boost）。
4. 其它
记忆关闭时仍会算 _query_mode，boost 列表为空，行为与以前一致。
.env.example 里加了一行说明检索模式是自动分类的。
记忆机制（当前整体）
User 长期记忆：仍是 runs/user_news_memory.json（profile、关键词、板块、排除信源）；跑成功后 LLM 合并 不变。
使用时机：
显式主题轮：只用本轮意图做检索合并；User 里旧关键词 不会 并进 GNews/意图关键词；仍会过滤排除信源。
泛化浏览轮：照旧合并偏好关键词/板块；并在 balance 之后 用偏好做 排位上移（加权重排）。
Context / Task：未单独落库；会话多轮仍只靠 Streamlit 聊天记录展示，未写入结构化 Context 文件（与之前一致）。
若你希望某类句式强制算 generic/explicit，可以说几句样例，我可以只改 classify_query_mode 里的正则规则。


【爬虫汇总】
1. 主流程里会用到哪些（AI 资讯 Agent / run_daily_digest / agent_runtime）
类型	实现位置（概要）	技术
RSS/Atom 拉取
run_daily_digest.collect_news → _collect_one_rss_source，信源来自 sources.json
urllib + 可配代理/超时（RSS_HTTP_TIMEOUT）
站点列表爬取（Playwright）
ai_news_skill/crawlers/site/*.py，经 registry.crawler_by_source_name 与 sources.json 的 name 精确匹配
Chromium + BeautifulSoup 解析列表/详情
正文兜底
fetch_article_excerpt → attach_content_excerpts_to_items
HTTP 拉 HTML + trafilatura（不是 Playwright）
GNews
fetch_gnews_articles / fetch_gnews_for_pipeline 等
GNews HTTP API
Public API 扩展信源
ai_news_skill/integrations/public_api_feeds.py
各官方 API（NYT、Guardian、HN、GitHub…）
OpenClaw 热榜
fetch_openclaw_stars_top（配置开启时）
HTTP 调用
也就是说：除了 RSS，主流程里确实还有「Playwright 站点爬虫」和「非 Playwright 的正文抓取」；不是只跑 RSS。

2. Playwright 站点爬虫清单（注册表里的 5 个）
实现目录：project/ai_news_skill/crawlers/site/
注册：project/ai_news_skill/crawlers/site/registry.py（与 project/site_crawlers/registry.py 内容一致，是同一套爬虫的注册表副本）。

类名	name（须与 sources.json 一致）	说明
OpenAINewsCrawler
OpenAI Blog
列表页 + 滚动，与 RSS 是两条路（代码里写明列表是 openai.com/news，不是 rss.xml）
AnthropicNewsCrawler
Anthropic Blog(OpenRSS)
Playwright 滚动 + 「See more」类交互；详情页再抓
HuggingFaceBlogCrawler
Hugging Face Blog
Playwright 滚动 + 详情
TechCrunchAICrawler
TechCrunch AI
Playwright
VentureBeatAICrawler
VentureBeat AI
Playwright
公共基座
base.py
playwright_chromium_launch_kwargs()、SITE_CRAWLER_HEADLESS、代理 playwright scope
何时会跑这些 Playwright 爬虫？
agent_runtime.site_crawlers_effective() 为真时：在 collect_stage 里对「有注册爬虫」的 sources.json 名称做 site_crawl；若 DIGEST_FAST_MODE=1 / DIGEST_FAST=1 或 SITE_CRAWLERS_ENABLED=false，则 整段跳过，trace 里会看到 site_crawl skipped（你上次 E2E 就是这种情况）。

3. 非 Playwright、但属于「抓取/拉取」的工具
名称	位置
HTTP + trafilatura 正文
run_daily_digest.fetch_article_excerpt
独立测试脚本：单 URL Playwright + trafilatura
project/test_harness_crawl/crawl_and_save.py
实验/样例脚本
project/scripts/run_openai_keyword_sample.py；测试/Anthropic官网列表按关键词model抓取.py
旧版/重复
project/site_crawlers/venturebeat.py 等（与 ai_news_skill/crawlers/site 体系并行存在，需看入口是否引用）
当前工作区 ai_news_skill/crawlers/ 下没有 google_hk_playwright.py（若你历史里改过该文件，可能在本机未提交或已删除）。

